# Agent Smith Web

Placeholder for the future React/Vite test client.

The current local test UI still lives in `test_app/static` and is served by
`agent_smith.transports.http` while the backend refactor settles.

